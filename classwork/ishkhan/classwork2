1. Надипсичить новый html
который бы отображал в списке имя учителя, возраст и курс, можно с css

2. добавить этот html в main/templates

3. добавить в html синтаксис Django templates

4. заменить в контроллере
    return HttpResponse(f"...")
    на
    render('название файла с html', context={???})

